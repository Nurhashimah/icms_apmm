<h3>New Course (Programme)</h3>

<% form_for(@programme) do |f| %>
  <%= f.error_messages %>

     <%=render :partial => 'form', :locals => {:f => f} %> 
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>


<%= link_to 'Back', programmes_path %>